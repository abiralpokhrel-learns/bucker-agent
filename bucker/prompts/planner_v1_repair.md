Your previous response was not a valid Task contract.

Validation errors:
$errors

Your previous response:
$previous

Return the corrected JSON object and nothing else — no prose, no markdown
fences. Fix only what the errors identify; keep everything else unchanged.
